A pokemon robot designed for 6v6 singles that uses the poke-engine library to make decisions based on the current game state. It is designed to be used with the poke-engine-ffi crate, which provides a C-compatible interface to the poke-engine library.

Check SETUP.md for instructions on how to set up the project and build the FFI crate.